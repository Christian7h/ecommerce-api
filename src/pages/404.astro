---
import '../styles/global.css';
---

<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Página no encontrada - 404</title>
  </head>
  <body class="bg-gray-100 min-h-screen flex items-center justify-center p-4">
    <div class="max-w-md w-full bg-white rounded-lg shadow-xl p-8 text-center">
      <div class="mb-8">
        <h1 class="text-6xl font-bold text-gray-800 mb-4">404</h1>
        <p class="text-2xl font-semibold text-gray-600 mb-2">¡Ups! Página no encontrada</p>
        <p class="text-gray-500">La página que estás buscando no existe o ha sido movida.</p>
      </div>

      <div class="space-y-4">
        <button 
          onclick="window.history.back()" 
          class="w-full bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-4 rounded-lg transition duration-300"
        >
          Volver atrás
        </button>

        <a 
          href="/" 
          class="block w-full bg-gray-100 hover:bg-gray-200 text-gray-700 font-semibold py-2 px-4 rounded-lg transition duration-300"
        >
          Ir al inicio
        </a>
      </div>

      <div class="mt-8 pt-6 border-t border-gray-200">
        <p class="text-sm text-gray-500">
          ¿Necesitas ayuda? 
          <a href="/contacto" class="text-blue-500 hover:text-blue-600">Contáctanos</a>
        </p>
      </div>
    </div>

    <script>
      // Registrar el error 404 para análisis
      console.error(`404 Error: ${window.location.pathname}`);
    </script>
  </body>
</html>